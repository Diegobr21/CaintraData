@page "/importdatausu"



 <h3>Importar Usuarios </h3>

<label>Seleccionar archivo: </label>
<InputFile OnChange="OnFileUploaded"></InputFile>
@if (FileEntry != null)
{
    <p>Name: @FileEntry.Name</p>
    <p>Size: @FileEntry.Size bytes</p>
    <p>Last modified: @FileEntry.LastModified.ToShortDateString()</p>
    <p>Content type (not always supplied by the browser): @FileEntry.Type</p>
}
<br />
<button disabled="@(FileEntry is null)" @onclick="@UploadFileContentsAsync">Cargar archivo</button>

<hr />
<!--
@if (FileEntry != null)
{
    <p>Name: @FileEntry.Name</p>
    <p>Size: @FileEntry.Size bytes</p>
    <p>Last modified: @FileEntry.LastModified.ToShortDateString()</p>
    <p>Content type (not always supplied by the browser): @FileEntry.Type</p>
}
<br />
<button @onclick="@UploadFileContentsAsync">Cargar archivo</button>

<hr />
-->
